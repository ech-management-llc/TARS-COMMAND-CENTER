{
  "schema_version": 1,
  "tenant_id": "ech",
  "name": "ECH Management",
  "reference_install": true,

  "branding": {
    "title": "FOUNDATION LAYER",
    "title_accent": "Command Center",
    "logo": null,
    "accent": "#10b981",
    "theme": "dark"
  },

  "operator": {
    "name": "Jerry",
    "greeting_name": "Jerry"
  },

  "ai_summary": {
    "enabled": true,
    "live": false,
    "attention": [
      { "t": "3 capital-rule checks can't run yet", "d": "LTV, DSCR and per-door are dark because there's no loan-balance or door-count source wired. Liquidity is the only rule computing today. These light up when the ledger leaves sandbox at Plaid production (Nov gate)." }
    ],
    "brief": [
      { "t": "Financial Health: liquidity is GREEN, $366K over the floor", "d": "Liquidity passes with $366K of headroom above the floor. LTV, DSCR and per-door are still pending (sandbox). Tap into Financial Health for the per-rule breakdown." },
      { "t": "Market is live: cap rate + two vacancy reads for your counties", "d": "Reventure cap rate and Reventure/Census vacancy are live for the counties you track. The seasonal gap explains why total vacancy and rental vacancy differ — your rental number is the one to underwrite to." },
      { "t": "Money figures read $0 until Plaid production", "d": "Revenue and NOI honestly show $0 while Plaid is in sandbox — nothing is fabricated. Real dollars flow at the November compliance gate." }
    ],
    "note": "Static for v1 — not a live model call. The live daily brief writes itself when the global agent is wired to a model."
  },

  "global_agent": {
    "name": "TARS",
    "role": "company-wide employee · all layers",
    "avatar": "T",
    "tagline": "your on-call AI employee · sees every layer · remembers your business",
    "intro": "Hi Jerry — I am TARS, your company-wide employee. I can see every layer and I load your knowledge folder each session, so you never re-explain. What do you need?",
    "chips": [
      "What needs my attention today?",
      "Find the lease for 415 Skiles",
      "Build a lender packet",
      "Who is late on rent?"
    ],
    "scope": "all"
  },

  "memory_store": {
    "provider": "cloud",
    "label": "ECH knowledge vault",
    "location": "tenant-configured (cloud)",
    "status": "configured",
    "writeback_mode": "draft-for-approval",
    "note": "Source of truth = the storage folder. The Document Navigator is the indexed VIEW of this same store, not a separate copy. Backup = scheduled mirror to a second location. Live read/append wiring lands with the agent model connection."
  },

  "data_sources": {
    "fl_api": "https://api.foundationlayerhq.com/api/dashboard/latest"
  },

  "market": {
    "default_county": "henderson",
    "picker_label": "📍 County"
  },

  "entitlement": {
    "mode": "all",
    "note": "v1 stub — ECH is entitled to every layer. Core still filters tiles by entitled:true so the licensing boundary exists in code."
  },

  "deploy": {
    "production_url": "https://tcc.echmanagement.services",
    "plaid_stage": "sandbox",
    "money_state": "placeholder_zero"
  }
}
